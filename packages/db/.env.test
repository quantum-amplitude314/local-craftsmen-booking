# Committed test value for db tests and `apps/api` `test:auth`. Must be a localhost database whose
# name ends with _test; the tests wipe it.
TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/craftsmen_test
