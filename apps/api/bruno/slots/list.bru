meta {
  name: list slots
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/slots?craft=painter
  body: none
  auth: none
}

headers {
  Cookie: {{sessionCookie}}
}

params:query {
  craft: painter
}

vars:post-response {
  craftsmanId: res.body[0]?.craftsman.id
}

assert {
  res.status: eq 200
  res.body: isArray
  res.body[0].craftsman.craft: eq painter
}
